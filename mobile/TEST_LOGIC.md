# Логіка тестів у мобільному додатку

## Загальна інформація

Тести - це щоденні тести з 5 питань, які генеруються автоматично для кожного користувача.

## API Endpoints

### 1. Генерація тесту
- **POST** `/api/daily-tests/generate`
- Генерує новий щоденний тест для користувача
- Повертає об'єкт тесту з `_id`, `date`, `status`, `questions`

### 2. Завантаження тесту
- **GET** `/api/daily-tests/{testId}/results`
- Завантажує тест з результатами (якщо є)
- Повертає:
  - `_id` - ID тесту
  - `date` - дата тесту
  - `status` - статус (`pending`, `completed`)
  - `questions` - масив питань з відповідями користувача
  - `score` - кількість правильних відповідей (0-5)
  - `coinsEarned` - нараховані монети

### 3. Відправка відповіді
- **POST** `/api/daily-tests/{testId}/answer`
- Body:
  ```json
  {
    "questionIndex": 0,
    "answerIndex": 1
  }
  ```
- Повертає:
  ```json
  {
    "success": true,
    "data": {
      "isCorrect": true,
      "coinsEarned": 10, // Кількість монет налаштовується через адмін панель
      "explanation": "Пояснення (якщо неправильно)",
      "allAnswered": false // true якщо це останнє питання
    }
  }
  ```
- **Примітка**: Кількість монет (`coinsEarned`) нараховується згідно з налаштуваннями адмін панелі і може відрізнятися для різних питань або тестів

### 4. Рейтинг
- **GET** `/api/stats/rating?position={positionId}`
- Повертає список користувачів, відсортованих за рейтингом по посаді

## Структура даних

### Test Model
```dart
class TestModel {
  final String id;
  final DateTime date;
  final String status; // 'pending' | 'completed'
  final List<TestQuestion> questions;
  final int score;
  final int coinsEarned;
}
```

### TestQuestion Model
```dart
class TestQuestion {
  final Question question;
  final int? userAnswer; // індекс відповіді користувача
  final bool? isCorrect; // чи правильна відповідь
}
```

### Question Model
```dart
class Question {
  final String id;
  final String text;
  final String? image; // URL зображення
  final List<Answer> answers;
  final String? explanation; // пояснення (якщо неправильно)
}
```

### Answer Model
```dart
class Answer {
  final String text;
  final bool isCorrect;
}
```

## Логіка TestScreen

### 1. Завантаження тесту
- При відкритті екрану завантажується тест через `GET /daily-tests/{testId}/results`
- Встановлюється дедлайн: 00:00 наступного дня (тільки для не-адміністраторів)
- Відображається таймер з часом, що залишився

### 2. Відображення питання
- Показується поточне питання (індекс з `currentQuestionIndex`)
- Відображається прогрес-бар: `(currentQuestionIndex + 1) / 5`
- Показується текст питання
- Якщо є зображення:
  - Відображається з можливістю натискання для збільшення
  - При натисканні відкривається модальне вікно з повнорозмірним зображенням
  - Під зображенням текст "👆 Натисніть для збільшення"

### 3. Вибір відповіді
- Користувач натискає на варіант відповіді
- Відповідь блокується після вибору (`selectedAnswer !== null`)
- Відправляється запит `POST /daily-tests/{testId}/answer`
- Після отримання відповіді:
  - Показується результат (правильно/неправильно)
  - Якщо правильно - показується кількість нарахованих монет (з API, налаштовується через адмін панель)
  - Якщо неправильно - показується пояснення та кнопка "Перейти до бази знань"
  - Анімація fade для показу результату

### 4. Перехід до наступного питання
- Через 3 секунди після показу результату:
  - Якщо `allAnswered === true` - перехід на `TestResultsScreen`
  - Якщо ні - перехід до наступного питання з slide анімацією
- Оновлюється `currentQuestionIndex`
- Очищається `selectedAnswer`, `showResult`, `resultData`

### 5. Блокування навігації
- Блокується кнопка "Назад" (hardware back button)
- При спробі вийти показується Alert з підтвердженням
- Якщо підтверджено - повернення назад (прогрес не зберігається)

### 6. Таймер
- Відображається в форматі `HH:MM:SS`
- Оновлюється кожну секунду
- Коли час вийшов:
  - Для не-адміністраторів: Alert + повернення назад
  - Для адміністраторів: таймер просто зникає

### 7. Особливості для адміністраторів
- Можуть проходити тести навіть якщо вони завершені
- Можуть проходити тести навіть якщо час вийшов
- Таймер не блокує навігацію

## Логіка TestResultsScreen

### 1. Завантаження результатів
- Завантажується тест через `GET /daily-tests/{testId}/results`
- Якщо тест завершено (`status === 'completed'`):
  - Завантажується рейтинг через `GET /stats/rating?position={positionId}`
  - Знаходиться позиція користувача в рейтингу

### 2. Відображення результатів
- **Основний результат:**
  - Круговий індикатор з оцінкою `score/5`
  - Відсоток правильних відповідей
  - Текстова оцінка:
    - 5/5 - "Відмінно!"
    - 4/5 - "Добре!"
    - 3/5 - "Задовільно"
    - <3 - "Потрібно покращити"
  - Колір індикатора:
    - ≥4 - зелений (#4CAF50)
    - 3 - помаранчевий (#FF9800)
    - <3 - червоний (#F44336)

- **Статистика:**
  - Нараховані монети (якщо > 0)
  - Позиція в рейтингу (якщо тест завершено)

- **Детальна інформація:**
  - Для кожного питання:
    - Номер питання
    - Статус (✓ Правильно / ✗ Неправильно)
    - Текст питання
    - Зображення (якщо є)
    - Всі варіанти відповідей:
      - Ваша відповідь (підсвічена, якщо неправильна - червона)
      - Правильна відповідь (підсвічена зеленою)
    - Пояснення (якщо неправильно)

### 3. Кнопки дій
- "На головну" - перехід на HomeScreen
- "Переглянути статистику" - перехід на StatsScreen

## Логіка HomeScreen (генерація тесту)

### 1. Перевірка поточного тесту
- При завантаженні перевіряється, чи є поточний тест
- Якщо тест існує і не завершено - показується кнопка "Почати тест"
- Якщо тесту немає - показується кнопка "Згенерувати тест"

### 2. Генерація тесту
- При натисканні "Згенерувати тест":
  - Відправляється `POST /daily-tests/generate`
  - Отримується новий тест
  - Планується нагадування (якщо тест не завершено)
  - Перехід на `TestScreen` з `testId`

### 3. Нагадування
- Планується нагадування на 16:00 та 19:00 дня тесту
- Показується нагадування про невиконаний тест

## Анімації

### TestScreen
1. **Fade анімація** при показі результату:
   - Opacity: 1 → 0.5 → 1 (200ms кожен крок)

2. **Slide анімація** при переході між питаннями:
   - Поточне питання: translateX 0 → -SCREEN_WIDTH (300ms)
   - Наступне питання: translateX SCREEN_WIDTH → 0 (300ms)

3. **Zoom анімація** для зображення:
   - Модальне вікно з fade анімацією
   - Повнорозмірне зображення з можливістю закриття

### TestResultsScreen
- Анімація появи результатів (опціонально)

## Обробка помилок

1. **Помилка завантаження тесту:**
   - Alert з повідомленням "Не вдалося завантажити тест"
   - Можливість повернутися назад

2. **Помилка відправки відповіді:**
   - Alert з повідомленням з сервера
   - Розблокування вибору відповіді (`selectedAnswer = null`)

3. **Помилка завантаження результатів:**
   - Alert з повідомленням
   - Кнопка "На головну"

## Стани екрану

### TestScreen States
- `loading` - завантаження тесту
- `test` - дані тесту
- `currentQuestionIndex` - поточне питання (0-4)
- `selectedAnswer` - вибрана відповідь (null або індекс)
- `showResult` - показувати результат
- `resultData` - дані результату (isCorrect, coinsEarned, explanation)
- `timeRemaining` - час, що залишився (формат HH:MM:SS)
- `imageZoomVisible` - видимість модального вікна зображення

### TestResultsScreen States
- `loading` - завантаження результатів
- `test` - дані тесту з результатами
- `ratingPosition` - позиція в рейтингу (position, totalUsers)

## Особливості реалізації

1. **Блокування повторного вибору:**
   - Після вибору відповіді кнопки стають неактивними
   - `disabled={selectedAnswer !== null}`

2. **Візуальна індикація:**
   - Правильна відповідь: зелена рамка + галочка ✓
   - Неправильна відповідь: червона рамка + хрестик ✗
   - Вибрана відповідь: підсвічується

3. **Lazy loading зображень:**
   - Показується placeholder під час завантаження
   - Обробка помилок завантаження

4. **Адаптивність:**
   - Підтримка телефонів та планшетів
   - Responsive layout для різних розмірів екранів

## Примітки

- Тест завжди містить 5 питань
- Кожне питання має 4 варіанти відповідей (1 правильна)
- Монети нараховуються тільки за правильні відповіді
- Кількість монет за правильну відповідь налаштовується через адмін панель
- Дедлайн: до 00:00 наступного дня
- Адміністратори мають особливі права (можуть проходити завершені тести)

